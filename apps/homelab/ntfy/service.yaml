apiVersion: v1
kind: Service
metadata:
  name: ntfy
  namespace: ntfy
spec:
  type: LoadBalancer
  selector:
    app: ntfy
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
