- name: Apply gateway nftables rules
  ansible.builtin.command: nft -f /etc/nftables.d/gateway.nft
  changed_when: true

- name: Enable gateway nftables service
  ansible.builtin.systemd:
    name: gateway-nftables
    enabled: true
    daemon_reload: true
