# Declarative overrides applied to the ServerUtilities-generated configuration.
# Every property must already exist in the generated base file with the same type.
world {
    B:chunk_claiming=true
    B:chunk_loading=true
}
