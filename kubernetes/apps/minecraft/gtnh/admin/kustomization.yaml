apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - host-key-v1.sops.yaml
  - role.yaml
  - rolebinding.yaml
  - service.yaml
  - serviceaccount.yaml
configMapGenerator:
  - name: gtnh-admin-authorized-keys
    files:
      - authorized_keys
