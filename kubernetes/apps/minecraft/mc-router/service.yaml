apiVersion: v1
kind: Service
metadata:
  name: mc-router
  namespace: minecraft
spec:
  type: ClusterIP
  selector:
    app: mc-router
  ports:
    - port: 25565
      targetPort: minecraft
      protocol: TCP
      name: minecraft
